{
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm install && npm run dev"
  },
  "dependencies": {
    "csv-parser": "^3.2.1",
    "lucide-react": "^1.22.0",
    "papaparse": "^5.5.4",
    "recharts": "^3.9.1"
  }
}
